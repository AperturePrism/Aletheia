# Aletheia 迭代自检报告 —— I1 · M1 光谱摄入

**迭代**：I1 ｜ **范围**：模块 M1（L0 光谱摄入，Go 侧）
**自检依据**：`05` 契约 / `04` §4 的 DoD / `09` 十条红线 / `02` §2 设计原则
**自检时机**：编码完成时做了一次轻量自检（P1/P3，见 §二.0）；本报告为迭代末完整自检
**结论**：✅ **可继续** —— 无 🔴 P0；1 项 🟠 P1（部分达成，已说明）；2 项 🟡 P2 登记待办

---

## 一、DoD 达成核对

`04` §I1 的 DoD 原文三条，逐条独立验证（跑命令看输出，不用记忆）：

| # | DoD 条目 | 状态 | 验证命令 | 原始输出（摘要） | 判定 |
|---|---|---|---|---|---|
| 1 | 解析器测试覆盖率 ≥ 90%，含边界用例（前导数字守卫、ANSI 转义、schema 参数名变体） | ✅ | `bash scripts/coverage-go.sh 90` | `解析器行覆盖率（排除 testutil）：92.2%（阈值 90%）` → `OK (Q2)`；分包：nmap 93.2% / httpx 97.0% / nuclei 97.3% / spectrum 95.8% | ✅ |
| 2 | 相同输入两次运行产出字节级一致的光谱 | ✅ | `make test-determinism` | `TestParseDeterministic`（nmap/httpx/nuclei 三包）+ `TestFoldDeterministic` + `TestFoldSemanticEquivalence10k` 中的 3 次运行比对全 PASS（`Deterministic: true` marshal 口径） | ✅ |
| 3 | 10⁴ 行输入折叠后语义可查询性无损失 | ✅ | `go test ./core/spectrum/ -run 'TestExpandRoundTrip|TestFoldSemanticEquivalence10k' -v` | 10⁴ 行 `ExpandFolded(Fold(x))` 逐条 `DeepEqual` 相等；折叠前后 `LinesContaining` 查询集一致且与构造时 ground truth 对照 | ✅ |

**M1 §7 验收清单的其余条目**（模块规格的 7 条，与 DoD 互补核对）：

| 条目 | 状态 | 证据 |
|---|---|---|
| 三适配器在自建靶场端到端跑通 | ⚠️ **部分达成（已说明）** | 解析层端到端由 fixtures 驱动全绿；真实工具执行依赖 I3 的 Sandbox/Scope/Gateway（M1 §5 T1.7 原文：「依赖 I3（可部分 mock）」）。集成测试以进程内 mock 验证了 Execute 全链路编排（`go test ./core/ingest/ -run TestExecute -v`） |
| 解析器覆盖率 ≥90% 且边界用例全绿 | ✅ | 同 DoD① |
| 相同输入 3 次运行字节级一致 | ✅ | 同 DoD② |
| 折叠前后语义等价 | ✅ | 同 DoD③ |
| 参数化 Schema 消除命令行注入 | ✅ | `TestValidateValueInjection` / `TestValidateValuePathType` / `TestValidateValueForbiddenChars`：`; \| & $() \` ` ` 等 9+ 类敌意值全部拒绝；argv 以独立元素传递（无字符串拼接路径） |
| 版本不匹配 fail-closed | ✅ | `TestExecuteVersionMismatch`（9.01 ∉ [7.40,7.99] → `TOOL_VERSION_MISMATCH`）+ `TestValidateTool/version mismatch` |
| 零 LLM 调用 | ✅ | R5 核查（§三） |

**未达成项与根因**：
- 「自建靶场端到端」——外部依赖 I3（Sandbox 容器编排）与靶场环境。
  按 `10` §0「未达成但已说明可接受」登记为 🟠 P1，处置：I3 集成时把
  mock 依赖替换为真实 gRPC 客户端（I1-交接 §8 已给出动作）。

---

## 二、偏差清单

### 0. 轻量自检记录（编码完成时，`10` §6 建议项）

执行了 P1（契约漂移）与 P3（安全绕过）检测命令：

```
git diff main --stat -- "docs/05-接口契约与数据模型.md" proto/
→ （空输出，零改动）
grep -rn "INSERT INTO entity" --include="*.go" --include="*.py" core/ agents/
→ 无命中
grep -rn "CONFIRMED" --include="*.go" core/ingest core/spectrum
→ 无命中（L0 不产出 Finding，M1 §2.2 N1）
grep -rnE "TODO.*bypass|FIXME.*skip|HACK.*disable|temporarily" core/ingest core/spectrum
→ 无命中
```

### 1. 本次迭代全部偏差

| # | 类别 | 严重度 | 具体表现 | 定位 | 处置 |
|---|---|---|---|---|---|
| 1 | P10 交付物部分 | 🟠 P1 | 「自建靶场端到端」依赖 I3 交付物，本迭代以 mock 边界达成 | `core/ingest/service_test.go` | 已在 I1-交接 §1/§7 显式说明；I3 验收时补真实端到端 |
| 2 | P2 流程 | 🟡 P2 | `Makefile` 的 `test-determinism` 原状为恒失败的占位（pytest `-m determinism` 零匹配 → exit 5） | Makefile | 改为 Go 侧真实门禁 + Python 侧显式声明（I2 恢复 pytest 行）。**非放宽**：见 I1-交接 §6 #1 的论证 |
| 3 | P2 流程 | 🟡 P2 | `defaultVersionProbe` 的 exec 成功分支无自动测试（本机无真实工具，42.9%） | `core/ingest/service.go:81` | 探测失败路径已测且走 fail-closed；I3 容器内补齐 |

---

## 三、十条红线专项核查

**每条附实际执行的命令与原始输出。**

| 红线 | 状态 | 验证命令 | 原始输出（摘要） |
|---|---|---|---|
| **R1** 契约未擅自变更 | ✅ | `git log --oneline main..HEAD -- "docs/05-接口契约与数据模型.md" proto/` | **空输出**（本分支对 `05` 与 proto/ 零改动）；`make test-contract` 10 passed（gate 内含） |
| **R2** Q3/Q4/Q5 未放宽 | ✅ | 人工核对 + `git diff main --stat -- tests/ scripts/` | Q3/Q4/Q5 完整门禁属 I2/I3（I0 已标注），无可放宽对象；本迭代新增的 Q2/Q6 门禁是**收紧**（新增 `coverage-go.sh` 90% 阈值与确定性测试）；`redline` 占位未被触碰 |
| **R3** 测试未被削弱 | ✅ | `grep -rn "pytest.mark.skip\|pytest.mark.xfail\|t.Skip" tests/ core/` | 命中 2 处：① `tests/test_roe_schema.py:93` 的 `skipif(uv is None)` —— I0 已判定（判据是工具存在性，实测 `PASSED` 非 `SKIPPED`）；② 本迭代曾新增一个平台相关 `t.Skip`，**自检时已删除**（负例改由跨平台可靠的"文件占位目录"测试覆盖 `TestNewRawStoreRejectsFileAsDir`）。无断言放宽、无用例删除（本迭代全部测试为新增） |
| **R4** 无证据路径旁路 | ✅ | `grep -rn "CONFIRMED" --include="*.go" core/ingest core/spectrum` | 无命中 —— L0 只产出观测与实体草稿，Finding 状态机在 M4（I2），本迭代代码在类型层面接触不到 CONFIRMED |
| **R5** LLM 未参与命令行生成 | ✅ | `grep -rniE "openai\|anthropic\|llm" core/ingest core/spectrum`（仅命中注释中的规则引用）；`grep -rnE "Sprintf.*nmap\|.*argv" ` 无拼接；`go.mod`/`go.sum` `git diff main` **空**（零新依赖，无 LLM SDK） | argv 唯一生成路径是 `ParamSchema.RenderArgv`（参数按定义序渲染为独立元素）；`Execute` 接受的 `ToolRequest.params` 是 `map[string]string` 填槽值，接口层面不存在自由命令行字符串（05 §2.2 禁止事项） |
| **R6** agent 无终止权限 | ✅ | `grep -rnE "os\.Exit\|sys\.exit\|session\.close" --include="*.go" core/ingest core/spectrum` | 无命中（`os.Exit` 仅存在于 `cmd/` 入口，I0 已有） |
| **R7** Gateway 不可绕过 | ✅ | `grep -rniE "gateway.*disable\|privacy.*off\|disable_privacy" core/ingest core/spectrum web/src`（排除 gen/） | 无命中；本迭代代码对 Gateway 只消费（Tokenize/ScanOutbound），无任何开关或旁路；`core/log` 的 I0 脱敏出口未改动 |
| **R8** ScopeKernel 逻辑未改 | ✅ | `grep -rnE "scope.*(skip\|bypass\|ignore)" --include="*.go" core/ingest core/spectrum`；`core/scopekernel/` 目录 | 无命中；ScopeKernel 本体未实现（I3），本迭代唯一 scope 相关代码是 `ScopeChecker` 接口消费 —— DENY 显式转为 `SCOPE_VIOLATION` 向上传播（fail-closed），不存在跳过路径（`TestExecuteScopeDenyFailsClosed` 锁定） |
| **R9** 三个核心视图完整 | ✅（N/A） | `git diff main --stat -- web/` | 本迭代无 web/ 改动；三视图属 I2（证据链）/ I5（光圈）/ I8（冲突矩阵），`04` §I1 无「前端交付物」行 |
| **R10** 未跳越阶段门禁 | ✅ | 人工核对 `04` §2/§4 + `ls agents/` | I1 属 P0（I0–I3），未触碰 P1 能力（`agents/` 下无 aperture/focalplane 等实现目录）；`agents/src/aletheia/` 仍只有 proto 生成物 |

---

## 四、仅人工判断的项（诚实声明）

| 项 | 为何无法自动化 | 判断依据 |
|---|---|---|
| 适配器版本区间声明合理（nmap 7.40–7.99 / httpx 1.3–1.6.99 / nuclei 3.0–3.99.99） | 需真实多版本工具实测，本机未装 | 基于输出结构稳定性判断（-oX / -json 字段结构）；已在 I1-交接 §7 登记为缺口，I3/I11 实测校准 |
| fixtures 无真实目标信息 | 隐私判定无法完全自动化（Q5 的抓包验证属 I3） | 全部样本使用 RFC 5737 文档网段（192.0.2.0/24）与 `.test` 保留域；逐文件人工核验 |
| `fold=off` 调试开关未暴露为 CLI/配置项 | 属产品决策（M1 §8 说"提供"但未指定载体） | 当前 `Service.SetFoldOff` 方法 + `TestFoldOffDebugSwitch` 已锁定机制；载体随 I3 配置面统一做 |
| 折叠算法的"无损"定义对契约的解释 | 契约（05 §2.1）未规定折叠的合并条件 | 以 `ExpandFolded(Fold(x)) == x` 与语义查询等价性作为可验证判据（DoD③ 全绿）；定义已写入 I1-交接 §3.2，供后续 review |

---

## 五、结论与下一步

**结论**：✅ 可继续。

- DoD 三条全达成（92.2% 覆盖率、字节级一致性、10⁴ 行无损折叠）。
- 无 🔴 P0；无红线违规；一处新增的 `t.Skip` 在自检中被抓出并当场消除。
- 🟠 P1「靶场端到端依赖 I3」已说明并有明确处置路径，不阻塞 I2。

**必须本迭代内处理**：无（🟠 项已登记至 I3）。

**已登记待办（P2）**：
1. `defaultVersionProbe` 成功分支的容器内测试（I3）。
2. nmap `safeScripts` 白名单扩充走安全 review（安全负责人）。

**下一步**：满足进入 I2（M4 证据账本与四道闸门）的条件 ——
I1 交付面与 I2 的对接点（RawOutputRef 同构、exec_id 桩路径、
`test-determinism` 的 pytest 恢复点）已写入 `I1-交接.md` §8。
