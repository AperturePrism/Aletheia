# Aletheia 迭代自检报告 —— I3 · M6 范围内核与隐私网关

**迭代**：I3 ｜ **范围**：模块 M6（Scope Kernel / Privacy Gateway / 速率限制 /
授权补全 / CostGovernor 最小实现）+ Go 装配适配 + 页面 9
**自检依据**：`05` 契约 / `04` §4 的 DoD / `09` 十条红线 / `08` 授权规范
**结论**：⚠️ **需修正（1 项 🟠）后可继续** —— Q5 的真实靶场抓包验证未执行
（本机 docker 未安装，I0 已知环境事实）；其余 DoD 全部达成。

---

## 一、DoD 达成核对（04 §I3 三条）

| # | DoD 条目 | 状态 | 验证命令 | 输出摘要 |
|---|---|---|---|---|
| 1 | 越界测试 100% fail-closed（DNS rebinding / HTTP 重定向 / IPv6 编码等构造用例） | ✅ 达成 | `go test ./core/scopekernel/ -v` | 全绿：七类绕过形态逐一构造（十进制/八进制/十六进制 IP、IPv6 映射、userinfo、尾点、超长域名）→ 归一化后精确比对；rebinding（解析结果含越界 IP）整体拒绝；无 resolver 时 L3 fail-closed；exclude 优先 / 默认拒绝；限流独立计数 |
| 2 | OWASP Juice Shop 靶场全程开网关抓包零真实值 | ⚠️ **未达成（已说明）** | 本机 docker 未安装（I0 已知环境事实） | **补偿性验证**：`go test ./core/gateway/` 验证 Tokenize→ScanOutbound 组合必干净（Q5 的机制层）；真实靶场抓包为 I3 收尾项，docker 可用后执行（Q5 的 redline 目标已挂接 gateway 用例） |
| 3 | 无授权凭证进程非零退出 | ✅ 达成（I0 已交付，本迭代保持） | `make check-governance-guard` | 「alethd 保留无凭证不启动（08 §1.1）」OK；gate 全绿 |

**附加交付**：CostGovernor 最小实现（Q9「预算耗尽必熔断，无逃逸路径」的执行体：
预扣失败即拒 + 熔断后无逃逸 + 结算复核）；Ingest 的 Scope/Gateway 依赖
真实化（I1 预留接口，I3 接线）；ScopeService gRPC 适配（Check/LoadScope）。

---

## 二、偏差清单

| # | 类别 | 严重度 | 表现 | 处置 |
|---|---|---|---|---|
| 1 | P10 部分 | 🟠 P1 | Q5 真实靶场抓包未执行 | docker 安装后补（I3 收尾项；机制层已验证，靶场验证是环境依赖） |
| 2 | 边界决策 | 🟡 P2 | Sandbox 与 GovernorService gRPC 未做（04 §I3 清单未列） | Sandbox 顺延（Ingest 该依赖显式失败）；Governor 包级实现已入（P0 门禁第 5 条必要条件），gRPC 适配留 I6 |

---

## 三、十条红线专项核查

| 红线 | 状态 | 验证命令 | 输出摘要 |
|---|---|---|---|
| R1 契约未擅自变更 | ✅ | `git log main..HEAD -- "docs/05-*.md" proto/` | 空（I3 零契约改动；794f135 复核仍待项目负责人） |
| R2 红线未放宽 | ✅ | `make redline` | Q3（I2）+ Q4/Q5 Go 用例全绿；`pytest -m redline` 34 passed |
| R3 测试未削弱 | ✅ | `grep skip/xfail` | 零新增 skip |
| R4 无 Ledger 旁路 | ✅ | focalplane 未触碰 | I2 测试保持全绿 |
| R5 LLM 未拼命令行 | ✅ | `check-no-bypass` R5 规则 | OK |
| R6 agent 无终止权限 | ✅ | `check-no-bypass` R6 规则 | OK |
| R7 Gateway 不可绕过 | ✅ | `gateway` 包无开关；alethd 装配无条件启用（roe 有效即接） | 配置文件显式禁用路径不存在（默认强制） |
| R8 ScopeKernel 逻辑未改 | ✅（本迭代建立） | 匹配语义三行测试（exclude 优先/默认拒绝）+ 判定全部内聚于 Kernel；适配器零决策 | 结构保证 |
| R9 核心视图 | ✅ | 页面 9 只读形态落地；页面 4 不变 | tsc/eslint/build 绿 |
| R10 未跳越阶段门禁 | ✅ | Sandbox 容器编排未做（04 §I3 未列） | Ingest Sandbox 依赖显式失败 |

---

## 四、仅人工判断的项

| 项 | 依据 |
|---|---|
| 速率限制默认 120/min 的取值 | M6 §3.4 未定数值；防御纵深层非精确计量，待真实运行校准 |
| Q5 补偿性验证的充分性 | 机制层（Tokenize→Scan 组合干净）已测试；靶场抓包验证的是同一机制的端到端表现 —— 属环境依赖而非逻辑缺口 |
| Governor 固定窗口限流的精度 | M6 §6 未规定实现；预扣制语义已按 §6.2 实现 |

---

## 五、结论与下一步

**结论**：⚠️ 需修正后可继续 —— 🟠 P1（Q5 靶场抓包）是环境依赖项，不阻塞
下一迭代的开发工作，但 **P0 阶段门禁**（04 §2）第 4 条在 docker 就绪前不得视为通过。

**下一步**：I4 · M2 心智地图与棱镜分解（P1 阶段首迭代）。I3 收尾项：docker 安装 →
Juice Shop 靶场 → Q5 抓包验证 → 更新本报告。
