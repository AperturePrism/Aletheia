# Aletheia 迭代自检报告 —— I2 · M4 证据账本

**迭代**：I2 ｜ **范围**：模块 M4（L3 焦平面与证据账本，Python）+ Go 转发代理 + 页面 4
**自检依据**：`05` 契约 / `04` §4 的 DoD / `09` 十条红线 / `02` §2 设计原则
**结论**：✅ **可继续** —— 无 🔴 P0；1 项 🟠 P1（靶场端到端，I3 复验）；无新增 P2 遗留

---

## 一、DoD 达成核对（04 §I2 三条）

| # | DoD 条目 | 状态 | 验证命令 | 原始输出（摘要） |
|---|---|---|---|---|
| 1 | 伪造证据注入测试 100% 被拒（无 exec_id / 假 exec_id / 张冠李戴三类） | ✅ | `pytest tests/redline/ -q` | `14 passed`：类别① 4 组载荷、类别② 5 种伪造形态、类别③ 含逐次换 target 无漏网变体；全部拒绝 + 账本零痕迹 + P0 告警 |
| 2 | 状态机流转测试：每条边与每个拒绝分支 | ✅ | `pytest agents/tests/focalplane/ -q` | `45 passed`：HYPOTHESIS→CANDIDATE→(CONFIRMED 不可达)、G-2 失败→REJECTED、CONFIRMED→CANDIDATE 无正当理由被拒、G-4 pending 显式 |
| 3 | 归因错误触发 P0 告警并可回滚 | ✅ | 同上（TestAttribution） | 异 target 引用 → `ATTRIBUTION_MISMATCH` + 冲突断言记 HYPOTHESIS + 既有引用者标 needs_review + on_p0_alert 回调触发 |

**G-1 执行绑定（T4.2）**：验签在 append 路径强制（`Ledger` 构造器必需注入 verifier，
不存在不验签的账本实例）；假阴性防线有测试（合法签名不被误拒）。

**G-3 模式库反例（D1 对策）**：`patterns.audit_text` 对 "Shellcodes" /
"exploit completed" / "login:" / "23/23 ports exploited, all root" 等弱证据
在任何 kind 下零命中（`test_weak_evidence_never_strong`）。

**账本完整性（07 §T4.3）**：篡改/删除中间记录（测试扮演攻击者直改 SQLite）→
`verify_chain` 检出断点；`findings`/审计表不可变性的静态检查（无 UPDATE/DELETE 路径）。

---

## 二、偏差清单

| # | 类别 | 严重度 | 表现 | 处置 |
|---|---|---|---|---|
| 1 | P10 部分交付 | 🟠 P1 | 「在真实靶场端到端跑通」依赖 I3 Sandbox 签发真实 exec_id —— I2 的签名由测试注入 Sandbox 公钥验证 | I3 集成时替换真实签名链复验（I1 已有同类先例） |
| 2 | 过程修正 | 🟡 P2 | ci.yml coverage job 的 `continue-on-error: true` 是 I0 遗留（注释说 I1 起收紧但 I1 未执行） | 本次修正为阻断（I1 覆盖率 92.2% 已达标） |
| 3 | 过程修正 | 🟡 P2 | gate 尾注/静态检查未随 I2 交付状态更新 | 已同步（Q3 移出补齐清单；审计表规则精确化） |

**过程发现并当场消除的问题**：R6 扫描抓到 focalplane 入口 `SystemExit`（语义是
拒绝启动而非 agent 终止会话）→ 改走 argparse 错误路径，规则零改动；
`.gitattributes` 缺失导致 autocrlf 把工作区 Go/fixtures 文件 CRLF 化
（gofmt/解析器测试受扰）→ 全仓统一 LF + fixtures 禁转换。

---

## 三、十条红线专项核查

| 红线 | 状态 | 验证命令 | 输出摘要 |
|---|---|---|---|
| R1 契约未擅自变更 | ✅ | `git log main..HEAD -- "docs/05-*.md"`（空）；proto 有且仅有 `794f135 contract:` 前缀 commit（AppendEvidenceRequest 为 [derived] 消息的合法扩展，frozen 零改动；**待项目负责人完成双人 review 复核**） | `make test-contract` 10 passed |
| R2 Q3/Q4/Q5 未放宽 | ✅ | Q3 本迭代**新增**（14 用例）；Q4/Q5 仍 I3；`make check-no-bypass` 全过 | 红线只能新增不能放宽 |
| R3 测试未被削弱 | ✅ | `grep -rn "pytest.mark.skip\|xfail\|t.Skip" agents tests` | 零命中（skipif 仅 I0 既有的工具存在性判断）；新增测试 59 个 |
| R4 无证据路径旁路 | ✅ | 结构证明：Ledger 构造器强制注入 verifier + append 强制验签（无旁路）；`grep CONFIRMED` 命中均为注释/受 G-4 约束的状态机分支 | 45+14 测试锁定 |
| R5 LLM 未参与命令行生成 | ✅ | focalplane 无任何模型 SDK；argv 由 L0 生成（I1），M4 只消费 | grep 零命中 |
| R6 agent 无终止权限 | ✅ | `check-no-bypass` R6 规则全过（过程中抓到入口 SystemExit，已改 argparse 路径，规则零改动） | OK |
| R7 Gateway 不可绕过 | ✅ | focalplane 无脱敏开关；出站脱敏属 I3 | 未触碰 |
| R8 ScopeKernel 逻辑未改 | ✅ | `core/scopekernel/` 未实现未触碰（I3） | OK |
| R9 三个核心视图 | ✅（本迭代交付其一） | 页面 4 证据链视图落地（路由 /evidence）；tsc/eslint/build 全绿；树形链 + 交叉引用双向渲染 + 归因状态 | 信息密度不简化 |
| R10 未跳越阶段门禁 | ✅ | G-4/Arbiter（I7）未实现；P1 能力未触碰 | `agents/` 无 aperture/orchestration 等 |

---

## 四、仅人工判断的项

| 项 | 依据 |
|---|---|
| contract 变更（AppendEvidenceRequest.signed_exec_id）的合理性 | 变更理由与影响面在 commit 794f135；frozen 面零改动；**双人 review 的第二人 = 项目负责人，待复核** |
| G-3 模式库的最小可信集是否足够 | 每条模式带出处注释与正反例；扩展机制开放（M4 §8），扩展属安全决策需 review |
| 比例熔断最小样本量（MIN_SESSION_SAMPLES=10）的取值 | M4 §4.6 未定义小样本行为；1/1=100% 会压过 agent 阈值语义，取 10 为最小可用值，待真实运行数据校准（M4 §8） |

---

## 五、结论与下一步

**结论**：✅ 可继续。DoD 三条全达成；Q3 红线 CI 落地；无未说明偏差。

**下一步**：I3 · M6 范围内核与隐私网关（Q4/Q5 红线）—— focalplane 的
exec_id 签发方（Sandbox）在 I3 落地，届时把测试注入的公钥换成真实
Sandbox 公钥并复验归因链路；`make redline` 补 Q4/Q5。
